# DISC-03 blockers

**Checked:** 2026-08-27
**Package state:** structurally complete; release blocked.

| ID | State | Blocker | Evidence / impact | Required resolution | Owner / recipient |
| --- | --- | --- | --- | --- | --- |
| B-01 | `blocked` | No real current-build capture or inspected export exists. | The package is a plan, not proof media. | Mike approves controlled synthetic capture; inspect and review source/export frames. | Mike decision; proof-factory lead; owned-surface lead. |
| B-02 | `blocked` | No direct redacted agent retrieval has been inspected. | A still or generated simulation cannot prove the read-only product path. | Run the controlled synthetic retrieval after capture approval; redaction and second review required. | Proof-factory lead. |
| B-03 | `blocked` | Named campaign destination is not supplied. | CTA cannot be attributable or safely linked to the App Store. | DISC-05 supplies and validates a campaign link before any export uses it. | Demand-capture lead; owned-surface lead. |
| B-04 | `blocked` | Detailed community rules for the two named subreddits could not be confirmed unauthenticated. | The drafts have no posting permission. | Direct logged-in review immediately before any approval; record rule links/date and affiliation/link constraints. | Creator-seeding lead; Mike approves post. |
| B-05 | `blocked` | Higgsfield generation output remains unproved for this job. | Authentication and a 75-credit estimate are verified, but no successful generation, rights/data-use/provenance review, or exact credit-use approval exists. | Use the bounded action packet in `marketing/discovery/_run/creative-provider-action.md` and get Mike's exact cost/use approval before a generation request. | Mike decision; proof-factory lead. |
| B-06 | `blocked` | No platform-result or privacy-safe product-result coverage exists. | No performance claim can be made; first MCP tool use is a coverage gap. | Publish only after separate approval, then use `results-retro.md` with aggregate cohort-safe evidence. | Root coordinator / evidence auditor. |
| B-07 | `blocked` | Mike has not made the creative-production or publication decision. | Preparation does not authorize capture, provider use, distribution, outreach, or scheduling. | Choose `approve production`, `revise`, or `defer`; later approve each external action separately. | Mike / root coordinator. |

## No workaround rule

Do not replace B-01 or B-02 with generated UI, a mock terminal, a fabricated retrieval, an unlabeled composited app screen, or an uninspected old export. Do not replace B-04 with a guess about community rules. Do not replace B-05 with a provider call or spend. These substitutions would violate the slice boundary.
